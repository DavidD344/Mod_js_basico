
let doubleSpeed = function(velocity){// variaveis podem virar funções

    return velocity * 2
}

let newVelocity = doubleSpeed(40)

alert(newVelocity)

//funções tradicionais sao lidas antes do codigo ser executdo

// essa é uma forma de garantir que a função so vai ser lida após o codigo ser executado